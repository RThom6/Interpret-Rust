### What is this?

This file contains the grammar of the language I am creating this interpreter for. It shouldn't be too crazy but won't be half as thorough as your usual language. But that's fine because the main goals of this project are handling the interpreting and such, not the language itself.
In this example, words starting with capital letters are my Non-terminals and lowercase words are Terminals. Fully capitalise words represent literal types such as a number literal, a string literal, or an identifier. Each option is separated by a pipe '|' and any of the options can be picked when its Non-terminal is present.

### Language

Statement => IfStatement | WhileStatement | AssignmentStatement ;
AssignmentStatement => Identifier "=" Expression ;
IfStatement => "if" "(" Expression ")" "{" Statement "}" ( "else" "{" Statement "}" )? ;
WhileStatement => "while" "(" Expression ")" statement ;
Expression => Equality;
Equality => Comparison ( ( "!=" | "==" ) Comparison )* ;
Comparison => Term ( ( ">" | ">=" | "<" | "<=" ) Term )* ;
Term => Factor ( ( "-" | "+" ) Factor )* ;
Factor => Unary ( ( "/" | "*" ) Unary )* ;
Unary => ( "!" | "-" ) Unary | Primary ;
Primary => number | string | "true" | "false" | "null" | "(" expression ")" ;


### What is this branch??

I went overboard early on and created a boat load of different token types thinking about my own language.
Since I was following 'Crafting Interpreters' by Robert Nystrom I've sent myself into a crazy spiral where I now have no idea what my next move is because I went a bit overboard. This branch is to basically reel that in, get rid of the more random and complex stuff and follow the book a bit more truthfully.
